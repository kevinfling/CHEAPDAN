foreach(t test_weights test_ceemdan_1d test_ceemdan_2d test_rts)
    add_executable(${t} ${t}.c)
    target_link_libraries(${t} PRIVATE cheapdan m)
    add_test(NAME ${t} COMMAND ${t})
    set_tests_properties(${t} PROPERTIES LABELS "cheapdan")
endforeach()
