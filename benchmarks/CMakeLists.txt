foreach(bench bench_ceemdan bench_fracdiff bench_homomorphic)
    add_executable(${bench} ${bench}.c)
    target_link_libraries(${bench} PRIVATE cheapdan m)
    target_compile_definitions(${bench} PRIVATE _POSIX_C_SOURCE=199309L)
endforeach()
